use flake

export COMPOSE_PROJECT_NAME=proxenos

# Load .env.local into the shell so Docker Compose's x-app-env passthrough
# (LLM_*, etc.) carries real values into the containers. Without this, compose
# injects empty vars that shadow Symfony's own .env.local at runtime.
dotenv_if_exists .env.local
