# Roadmap — post-phase-0-roadmap

The plan of record for Post-Phase 0 Roadmap. Brief: [`BRIEF.md`](./BRIEF.md). Locked
decisions graduate to `engineering/decisions/` (ADRs) as soon as they lock;
this roadmap holds the plan; each Step gets a
`workplans/step-NN-<slug>.md` when it starts.

Started: 2026-06-16.

---

## Round 1 — _title_

_One-sentence shipping criterion for the round._

- **step-01 — F1 — Model-profile / task-intent taxonomy** — Shared prerequisite for both parallel lanes (A and D). Name the model-selection layer the spine, the operation registry, and every memory/truth feature will consume — **operator-level only** (ADR-008: no user-facing picker). Mostly a **design + minimal config refactor**: produce the ADR and seed `proxenos.model_profiles` with the intent taxonomy by restructuring the two profiles already in `config/packages/proxenos.yaml` (`chat.frontier`, `chat.frontier_alt`). **Resolved framings (encode, don't relitigate):** two layers already present — profile → `{platform, model, options}` (built; ADR-023) and feature → profile (lightweight env override, e.g. `PROXENOS_{FEATURE}_MODEL_PROFILE`). Two naming dimensions composed by convention: *intent* (`proxenos.task.{chat, reason, extract, summarize, embed.text}`) and *quality/latency variant* (`.fast` / `.deep` / `.frontier`). v0 = naming convention over one flat map; the resolver stays a dumb `name → profile` lookup. Bespoke profiles are first-class peers. Keep `platform:` (symfony/ai's term). `task.embed.text` is a different capability kind (no streaming, carries an embedding dimension) — the profile schema likely needs a `kind` marker. **Open sub-decisions to settle:** canonical intent list (`code` in v0 or reserved?); declare-all vs declare-as-needed in config (lean: ADR holds canonical list, config defines only profiles consumed now); per-profile schema fields; where feature→profile bindings live (config + env now, graduate into ADR-014 registry later); confirm quality variants stay naming-convention-only for v0. **Scope:** an ADR ("model-profile / task-intent taxonomy") recording the two layers, two dimensions, flat-map convention, bespoke-peer rule, `platform` term, embeddings `kind`, and the feature→profile binding pattern (cross-reference / lightly amend ADR-008 / ADR-014 / ADR-023); seed the taxonomy in `proxenos.model_profiles` (e.g. `chat.frontier` → `proxenos.task.chat`; keep the generic/OpenRouter profile as the bridge example); update the one caller default (`ChatRespondRequest::modelProfile`) and the test rebinding in `config/services_test.yaml`; document the feature→profile env pattern with a worked example. **Hard exclusions:** no user-facing picker (ADR-008); no composed (intent × quality) resolver; no `xes.model.*` indirection; no Operation Registry build (only the binding shape — registry is F2); no new providers/bridges beyond `anthropic` and `generic.default`. **Definition of done:** ADR recorded; profiles restructured into intent taxonomy with default caller + `services_test.yaml` rebinding updated; `make test` + `make lint` green; feature→profile env convention documented with a worked example; canonical intent list + open sub-decisions resolved in the ADR. Inputs / full handoff: [handoff-model-profile-taxonomy.md](../../../current-handoffs/handoff-model-profile-taxonomy.md). ✅ shipped 2026-06-16
<!-- wip-amend: 6f179faae995e2f594913aaedd710b98c6de11625e4bf4798e7808c3cda9b65b -->

<!--
Parallel lanes (ADR-0010): a round may run tracks in parallel. Put the shared
prereq in the main lane (a bare bullet), then one `### Lane <name>` subheading
per track. Lanes run in parallel; steps within a lane stay sequential. Step ids
stay globally sequential — the lane is a grouping, not a numbering namespace.

- **step-02 — shared prereq** — must land before either lane.

### Lane A
- **step-03 — track A work** — runs in parallel with Lane B.

### Lane B
- **step-04 — track B work** — runs in parallel with Lane A.
-->

### Lane A
- **step-02 — Track A — Vertical spine on `core.document`** — Build the first vertical cut through the typed-context layer **against a host-native `core.document` entity** — typed entity → schema render → reference resolution → attach → entity-aware prompt assembly → budget → the existing turn loop → stream. Proves the product thesis end to end **without opening the ADR-010 process boundary**. Opening ADR-010 and adding Hypomnema as the first *external* provider is the next slice (part 2), deliberately deferred so the spine isn't hostage to the separate Hypomnema project. **Path:** (1) `core.document` typed entity (ADR-013 schema + presentation hints) → (2) host-storage CRUD (Doctrine; ADR-017 baseline) → (3) reference resolution (ADR-013a envelope → instance + sidecar; exercise resolved/dangling paths) → (4) schema-driven render (ADR-012 card / pill, no escape hatch yet — but start the ADR-012 escape-hatch evidence list) → (5) attach / pin to a thread (a reference, not a copy; event-sourced) → (6) entity-aware prompt assembly (ADR-018 declaration v0, replaces `ChatRespondLoop`'s dumb history concat) → (7) budget planner admits it (ADR-016 v0 modes; `transclusions` class = no-op placeholder) → (8) `response.generate` operation-shaped on the existing loop, resolving the F1 `task.chat` profile. **Decisions to land:** `core.document`-first; reuse the existing loop (do NOT build the ADR-014 registry — that's F2; keep the call/return shape registry-adoptable); schema-driven rendering only (ADR-012, no code escape hatch); budget v0 (ADR-016 simple modes); prompt declaration v0 (lean, host-planned); write path minimal (at most one `core.document` create/update). **Transclusion guardrail (load-bearing, do not skip):** serialization routes through the expansion-policy slot (`pill | summary | full` from the reference envelope, ADR-013a) — the slice implements only `pill`/reference but **routes through the slot**, never hardcodes "references are pills, full stop." Keep ADR-016's `transclusions` budget class as a no-op placeholder rather than omitting it. **Hard exclusions:** no ADR-010 / JSON-RPC / Hypomnema / external providers (that's part 2); no transclusion expansion beyond `pill`; no Operation Registry build (ADR-014); no suggestion engine / attach picker UX beyond minimum / citation pills (Track D / later); no second entity type, no multi-user, no branching; no full ADR-018 ordering/cache-breakpoint machinery, no async ADR-016 compaction. **Definition of done:** a running slice (or a build plan with the interface contract at each numbered step); `core.document` declared, host-stored, resolvable via the reference envelope, schema-rendered as card/pill; a document reference attaches to a thread and is serialized into the prompt through the expansion-policy slot; `transclusions` budget class present as a no-op; the turn runs through the existing loop with entity-aware assembly, Phase 0.4/0.5 streaming + `assistant_turn_failed` behavior unchanged; `make test` / `make lint` green; first entry in the ADR-012 escape-hatch evidence list; uses the F1 `task.chat` profile. **Depends on:** step-01 (F1 — the `task.chat` profile that `response.generate` resolves). **Coordinates with Lane D on `ChatRespondLoop` prompt assembly** — see the bundle's cross-cuts: this lane swaps the dumb history concat for entity-aware assembly; Lane D prepends a system prompt. Agree the ordered-contributions contract up front. Inputs / full handoff: [handoff-core-document-spine.md](../../../current-handoffs/handoff-core-document-spine.md). ✅ shipped 2026-06-16
<!-- wip-amend: cb5646ed035a034ffb32eaa0e267220cda58cea3a1efde2f862f4e67a8f9a359 -->

### Lane D
- **step-03 — Track D — SPA usability v1** — Turn the bare assistant-ui Thread from PR #8 into an app you'd actually live in daily — the near-term Iris replacement for *core* chat use (conversations, threads, the chat surface, a stop button, a system prompt). Payoff of the ADR-026 cutover: mostly **frontend + thin backend**, touches **no ADR-010**, runs **parallel to Track A** (different code, additive surface). **Decisions to land:** build in the SPA, not Twig (this track is *why* the SPA exists; the Twig chat surface retires at the end); v1 scope is deliberately the daily-driver minimum (settings page, edit/branch UX, replay snapshot fallback, auth-UX polish are an explicit second pass); thread lifecycle is event-sourced (rename/archive are new conversation events folded into the projection — lean: explicit named event types like the existing four-plus-one vocabulary); cancellation is real backend work (cooperative stop in the loop + `assistant_turn_cancelled` event + Turn/Message `CANCELLED` fold, mirroring `assistant_turn_failed`); system prompt is a simple v0 (global default + optional per-thread override, prepended in assembly — not the full ADR-018 machinery); model selection stays operator-only (ADR-008 — no picker). **Scope (must-have to daily-drive):** (1) app shell & navigation — SPA as home (login → `/app`), client-side routing (`/app/threads/:id`), persistent layout (thread sidebar + active thread + top bar with user menu, sign out wired to `logout`, tenant indicator); (2) thread management — assistant-ui ThreadList over an external thread-list adapter, list ordered by `updatedAt`, new/switch, rename, archive/delete (events → projection), auto-title via F1 `task.summarize` (heuristic stand-in acceptable v0); (3) chat-surface polish — markdown + syntax-highlighted code (copy buttons), message copy action, autoscroll with "jump to latest", honest FAILED-turn rendering (PR #5), empty-thread + loading states; (4) stop button — the cancellation path; (5) system prompt / persona — global default + per-thread override, injected per the assembly contract; (6) retire the Twig chat stopgap — delete `ChatController`'s Twig actions, `templates/chat/show.html.twig` + the hand-rolled inline SSE parser, and the form-POST/SSE-companion routes (keep the Twig login page; auth-UX polish is second pass). **Hard exclusions (defer):** no edit / regenerate / branch UX (needs host branch repository); no settings page beyond the system-prompt affordance (theme, account, password — second pass); no model picker (ADR-008); no entity rendering / attach picker / citation pills (those are Track A's leaf renderers; this shell *consumes* them); no new providers, no ADR-010, no replay snapshot-plus-cursor fallback; no registration / password reset (ADR-020). **Definition of done:** SPA is the default entry point with shell, routing, thread sidebar, and a working sign-out; threads can be listed, created, switched, renamed, archived (each via an event folded to the projection) and a new thread gets an auto (or heuristic) title; the chat surface renders markdown/code with copy, autoscroll, empty/loading states, and FAILED turns honestly, streaming still works end-to-end; **Stop** cancels a running turn (cooperative stop + `assistant_turn_cancelled` event + `CANCELLED` fold; rebuild reproduces the cancelled state; credential-free test coverage); a system prompt (global + per-thread override) is editable and injected into assembly without disturbing Phase 0.4/0.5 streaming/failure behavior; the Twig chat surface is removed (no-JS login page stays); `make test` + `make front-test` + `make lint` + `make front-lint` green; the adapter/reducer reconciliation tests from PR #8 still pass. **Depends on:** step-01 (F1 — the `task.summarize` profile that auto-titles resolve; heuristic stand-in is an acceptable bridge if D wants to ship before F1). **Coordinates with Lane A on `ChatRespondLoop` prompt assembly** — see the bundle's cross-cuts: this lane prepends a system prompt; Lane A swaps the dumb history concat for entity-aware assembly. They compose (system prompt → entity context grants → history) — agree the ordered-contributions contract up front so the two workstreams don't clobber each other. Inputs / full handoff: [handoff-spa-usability-v1.md](../../../current-handoffs/handoff-spa-usability-v1.md). ✅ shipped 2026-06-17
<!-- wip-amend: 35775601382cc092fa218de4c9fb2a7fc8c78f484131b4c687b4cdec089e3e39 -->



---

## Deferred (decided-not-now)

_Items consciously postponed; keep the why so future-you can re-evaluate._

## Backlog (cross-cutting; see also `.wip/backlog.md`)

_Cross-cutting work that hasn't earned a round yet._
